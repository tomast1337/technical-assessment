import { AppLayout } from "@components/Layout/AppLayout";
import { RegionList } from "@components/Region/ListRegions";

export const RegionsListPage = () => {
  return (
    <AppLayout>
      <RegionList />
    </AppLayout>
  );
};
